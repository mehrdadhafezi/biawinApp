# Copy to .env.local for local development.
# Same backend as apps/web (one NestJS API serving both /auth/** and
# /admin/auth/**), but declared independently — see
# docs/admin-architecture-decision-record.md §11.
NEXT_PUBLIC_ADMIN_API_URL="http://localhost:4000/api/v1"
