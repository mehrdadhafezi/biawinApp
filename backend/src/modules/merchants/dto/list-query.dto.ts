import { PaginationQueryDto } from '../../../common/dto/pagination.dto';

/** No extra filters yet beyond pagination — extended when this module becomes a Feature. */
export class ListMerchantsQueryDto extends PaginationQueryDto {}
